const fs = require("fs");
const { fileURLToPath } = require("url");

fs.unlink("./HelloWorld.txt", (err) => {
    if(err){
        console.error(err);
    } else {
        console.log("Successfully deleted fileURLToPath.")
    }
});
